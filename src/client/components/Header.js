import React from 'react';

const IndexPage = () => (
    <header className="header">
        <a className="logo">
            INDIE JOBS
        </a>
        <button>
            Entrar
        </button>
    </header>
);

export default IndexPage;
